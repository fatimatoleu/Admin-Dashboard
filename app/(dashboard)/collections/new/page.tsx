import CollectionForm from "@/components/collections/CollectionForm"

// Импортируем компонент CollectionForm из указанного пути.

const CreateCollection = () => {
  // Определяем компонент CreateCollection как функциональный компонент.

  return (
    // Возвращаем JSX для рендеринга компонента CollectionForm.
    <CollectionForm />
  )
}

export default CreateCollection
// Экспортируем компонент CreateCollection по умолчанию.
